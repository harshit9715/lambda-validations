{
  "name": "sst-lambda-validation",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.105.0"
  },
  "dependencies": {
    "@aws-cdk/core": "1.105.0",
    "@serverless-stack/cli": "0.29.1",
    "@serverless-stack/resources": "0.29.1",
    "ajv": "^6.12.6"
  }
}
